Configuration
~~~~~~~~~~~~~

Configuration is done using a ``yaml`` file that supports interpolation of
environment variables. You can use the provided ``datapuntauth.yml`` file or
create your own. Note that all settings are required.
